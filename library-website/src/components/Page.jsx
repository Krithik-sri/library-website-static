import React from 'react';

const Page = ({ pageNumber }) => (
  <div>
    <h1>Page {pageNumber}</h1>
    <p>This is page number {pageNumber}. Content will be added later.</p>
  </div>
);

export default Page;
